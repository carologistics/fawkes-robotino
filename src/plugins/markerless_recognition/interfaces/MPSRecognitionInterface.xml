<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE interface SYSTEM "interface.dtd">
<interface name="MPSRecognitionInterface" author="David Schmidt" year="2016">
  <constants>
   <enum name="MPSType">
     <comment>The different types of machines.</comment>
     <item name="NoStationDetected">No station detected.</item>
     <item name="CS_I">Base station.</item>
     <item name="CS_O">Cap station.</item>
   </enum>
  </constants>
  <data>
    <comment>
      This interface allows to control mps recognition plugin, e.g. take data, compute machine type, clear taken data.
    </comment>
    <field type="bool" name="final">
      True, if the last recognition triggered by a ComputeMessage has
      been completed, false if it is still running. Also check the
      msgid field if this field applies to the correct message.
    </field>
    <field type="MPSType" name="mpstype">
      The result of the recognized mps.
    </field>
  </data>
  <message name="Clear">
    <comment>
      Remove all currently taken data.
    </comment>
  </message>
  <message name="Compute">
    <comment>Trigger recognition calculation.</comment>
  </message>
</interface>
